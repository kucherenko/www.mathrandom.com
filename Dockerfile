FROM node:14

ARG SENDGRID_API_KEY
ARG MAIN_LIST_ID
ARG RECAPTCHA_SITE_KEY
ARG RECAPTCHA_SECRET_KEY

WORKDIR /usr/src/app

COPY . ./
RUN yarn

EXPOSE 3000

ENV HOST=0.0.0.0
ENV PORT=3000

RUN ls
RUN yarn build

CMD [ "yarn", "start" ]
